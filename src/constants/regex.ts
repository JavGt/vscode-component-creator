/**
 * Regex para filtrar los folders que se muestran en input de la ruta
 * Regex for filtering folders that are displayed in the route input
 */
export const regexFolders = /^(?!\.)(?!node_modules)/;
